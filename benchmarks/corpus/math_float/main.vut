import math

fn main():
  total = 0.0
  i = 0
  for i < 5000000:
    x = i.to_float()
    total = total + math.sqrt(x) + math.abs(x - 3.0) + math.floor(x) + math.ceil(x) + math.trunc(x)
    i = i + 1
  out("$(total.to_int())")
