fn apply(value: int, callback: fn(int) -> int) -> int:
  callback(value)

fn main():
  inc: fn(int) -> int = x => x + 1
  total = 0
  i = 0
  for i < 1000000:
    total = apply(total, inc)
    i = i + 1
  out("$total")
