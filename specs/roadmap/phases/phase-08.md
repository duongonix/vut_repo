# Phase 08 — Functions and Methods

## Status

Complete

## Completed Work

Implemented typed signatures, positional/named argument validation, duplicate and
missing argument diagnostics, explicit and final-expression returns, recursion,
static method lookup, method privacy, and compiler-injected `self`. Methods retain
the canonical `fn Type.method(...)` syntax exclusively.

## Tests

- [x] Function/method calls, named arguments, return mismatches, receiver lookup,
  implicit/invalid `self`, privacy, and compile-pass/fail method syntax.
