1. Payload enum + pattern matching đầy đủ.
2. Generics + constraints + monomorphization (nền tảng cho collections, iter, option).
3. Capturing closures.
4. Optional narrowing/unwrapping; numeric literal formats; loop labels; default args.
5. Static/extension methods; operator overloading; user-defined destructors/resource model.
P2 — Standard library theo specs/std/04-roadmap.md:
path → time → os → env → io → fs → process → math/random/ffi → collections/iter/cmp/hash/fmt/unicode/encoding → net → terminal/signal (hardening ở cuối).
P3 — Concurrency/async: executor single-thread thật + async I/O/cancellation; sau đó threads/sync/channel/task/structured concurrency.
P4 — Tooling/ecosystem: LSP đầy đủ, REPL, debugger, docs/explain, publishing/auth.
P5 — Hardening 1.0: exhaustive tests, sanitizers/fuzz, đa nền tảng, chốt ABI + compatibility.