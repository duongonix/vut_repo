import time

fn main():
  stamp = time.now()
  out("epoch seconds: $(stamp.seconds())")
  out("epoch milliseconds: $(stamp.milliseconds())")

  start = time.instant()
  time.sleep(time.milliseconds(5))
  finish = time.instant()

  span = time.elapsed(start, finish)
  out("elapsed milliseconds: $(span.milliseconds())")

  one_second = time.seconds(1)
  out("one second in milliseconds: $(one_second.milliseconds())")
